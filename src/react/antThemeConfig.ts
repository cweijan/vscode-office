import { ThemeConfig } from "antd";

export const antThemeConfig: ThemeConfig = {
    token: {
        controlHeight: 28,
    },
    components: {
        Button: {
            paddingInline: 8,
        },
    }
}

